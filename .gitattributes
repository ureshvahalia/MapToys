# Normalize all text files to LF in the repo and on checkout
* text=auto eol=lf

# Binaries — no line ending conversion
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.icns binary
*.wasm binary
*.node binary
